# dependencies
node_modules/

# build output
frontend/dist/

# env / secrets
.env
.env.local
.env.*.local
backend/.env

# uploads (runtime only - never in source)
backend/uploads/
!backend/uploads/.gitkeep

# backups (timestamped pg_dumps — keep them off git)
backend/backups/

# logs & system noise
*.log
npm-debug.log*
.DS_Store
.idea/
.vscode/
